syntax = "proto3";

service RPCHook {
  rpc DoubleWayStreamFun(stream RequestMessage) returns (stream ResponseMessage){}

    rpc doBeforeRequest(final String remoteAddr, final RemotingCommand request);

}
