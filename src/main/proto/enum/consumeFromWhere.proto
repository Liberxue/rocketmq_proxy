syntax = "proto3";

enum ConsumeFromWhere {
    CONSUME_FROM_LAST_OFFSET =0;
  //  CONSUME_FROM_LAST_OFFSET_AND_FROM_MIN_WHEN_BOOT_FIRST=3;
  //  CONSUME_FROM_MIN_OFFSET=5;
  //   CONSUME_FROM_MAX_OFFSET=7;
    CONSUME_FROM_FIRST_OFFSET=1;
    CONSUME_FROM_TIMESTAMP=2;
}